/**
 * Canonical user-facing strings for Fraudly (English-only production).
 *
 * Prefer importing from here in new code instead of scattering literals.
 * Future i18n: replace exports with locale-aware accessors (or split `messages.nl.ts`).
 */
export { EN_MESSAGES as uiCopy } from "@/lib/messages.en";
